#include <stdio.h>

/**
 * main - Prints numbers from 0 to 9.
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int t;

	t = 48;
	while (t < 58)
	{
		putchar(t);
		t++;
	}
	putchar('\n');
	return (0);
}
