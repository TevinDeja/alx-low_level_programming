#include "main.h"

/**
 * _abs - finds the absolute value of a no
 * @a: parameter to be checked
 *
 * Return: Always -a or a i
 */

int _abs(int a)
{
if (a < 0)
return (-a);
else if (a >= 0)
{
return (a);
}
return (0);
}
